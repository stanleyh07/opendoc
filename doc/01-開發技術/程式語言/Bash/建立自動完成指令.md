1. 建立自動完成指令參數
   例如: build.sh [uboot | kernel | recovery | updateimg ]
```
   complete -W "uboot kernel recovery updateimg" build.sh
```

2. 列出支援那些自動完成指令
```
   complete -p build.sh
```

3. 若要移除自動完成
```
   complete -r build.sh
```
