要暫時關閉 Docker 容器的控制台，但仍讓容器繼續執行，您可以使用 `Ctrl+P` 然後按 `Ctrl+Q` 的快捷鍵。這樣您將會脫離容器的控制台而容器繼續在後台運行。稍後，您可以使用 `docker attach` 重新連接到容器。具體步驟如下：

1. 當您已經在容器的控制台內時，按下 `Ctrl+P` 然後按 `Ctrl+Q`。這樣會讓您脫離容器的控制台而不會停止容器。
    
2. 要重新連接到容器，您可以使用以下命令：
    ``` sh
    docker attach <容器名稱或ID>
    ```

這樣您就可以暫時關閉控制台，而容器依然在運行。