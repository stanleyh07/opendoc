# 專案規則

## 語言要求

- 所有的思考過程必須使用繁體中文
- 所有的對話回應必須使用繁體中文
- 程式碼註釋可以使用英文，但說明和對話必須使用繁體中文

---

# 專案知識管理規則

本專案的知識文件位於 `doc/` 目錄，所有文件新增與修改必須遵循以下規則：

## 核心規則

1. **語言**：所有文件必須使用**繁體中文**撰寫。專有名詞可保留原文對照。
2. **分類歸檔**：新文件必須依 [[doc/00-知識庫核心/目錄結構與分類標準]] 的分類架構放入對應目錄。無合適分類時才新增，新增後須更新分類文件。
3. **新增前檢查**：新增任何文件前，必須先搜尋 `doc/` 全目錄確認是否已有相關內容。若有，應整合至既有文件，**不得刪減原有內容**，只能補充擴充。高度相關但主題明確可分者，使用 `[[wikilink]]` 關聯。
4. **Obsidian 格式**：必須包含 YAML frontmatter（title, tags, created, modified）、使用 `[[wikilinks]]` 內部連結、支援 Callout 區塊。
5. **詳細規範**：見 [[doc/00-知識庫核心/知識管理規則]]。

---

# 自訂指令

## 指令一：文件問答

當你收到類似「閱讀文件 XXX」、「回答關於 XXX 的問題」等要求時，應使用此指令：

1. **確認目標**：確認用戶要閱讀的文件路徑或目錄名稱
2. **讀取內容**：使用 `Read` 工具讀取指定文件（若為目錄則先列舉內容）
3. **理解與回答**：根據文件內容回答用戶的問題，必要時引用出處（檔案路徑與行號）

範例使用方式：
- 「閱讀 doc/01-開發技術/Python 的內容」
- 「幫我看 doc/00-知識庫核心/知識管理規則.md 的第三章」
- 「回答關於知識分類標準的問題」

## 指令二：知識庫寫入

當你收到類似「將這段對話加入知識庫」、「整理到知識庫」等要求時，應使用此指令：

1. **回顧對話**：檢視對話內容，提取關鍵資訊與知識點
2. **分類歸檔**：
   - 先搜尋 `doc/` 全目錄確認是否已有相關內容
   - 依 [[doc/00-知識庫核心/目錄結構與分類標準]] 決定分類位置
   - 若已有相關內容，應整合至既有文件，不得刪減原有內容
3. **執行寫入**：
   - 新文件：遵循 Obsidian 格式（YAML frontmatter、wikilinks、Callout 等）
   - 既有文件：以新增章節的方式補充，保留完整脈絡
   - 更新 frontmatter 中的 `modified` 日期
4. **關聯維護**：使用 `[[wikilink]]` 建立文件間的連結關係

範例使用方式：
- 「將剛才的 Python 技巧整理到知識庫」
- 「把這段對話關於 Git 的內容存起來」
- 「將這個設計模式筆記加入知識庫」
